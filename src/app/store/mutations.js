export default {
  profile(state, mut) {
    state.profile = mut;
  },
  loaded(state, mut) {
    state.loaded = mut;
  },
  messageStatus(state, mut) {
    state.messageStatus = mut;
  },
  bgShown(state, mut) {
    state.bgShown = mut;
  }
};
